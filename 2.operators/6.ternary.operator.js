/**
 * @description Ternary Operators
 *
 * evaluates to more than one condition to assign a value
 */

/**
 * If a customer has more than 100 points,
 * they're a "gold" customer, otherwise,
 * they're a "silver" customer
 */

let points = 110;
let type = points > 100 ? "Gold" : "Silver";

console.log(type);

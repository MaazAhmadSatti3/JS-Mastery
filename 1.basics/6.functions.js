/**
 * @description functions
 */

// A function to diplay greet message with user name parameter
function greet(name) {
  console.log("greetings from " + name);
}

// calling the function and passign argument
greet("maaz");
